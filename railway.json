{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'alembic upgrade head && if [ \"${RESET_CALENDAR_ON_DEPLOY:-0}\" = \"1\" ]; then python scripts/clear_calendar_data.py; fi && python scripts/ensure_admin_once.py && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
